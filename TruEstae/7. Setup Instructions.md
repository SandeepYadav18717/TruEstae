

#  Complete Setup Instructions - TruEstae Project

## 🏗️ Project Overview
**Full-stack real estate application** with:
- **Backend**: Python FastAPI (runs on **PORT 5000**)
- **Frontend**: React 19 + Vite (runs on PORT 5173)
- **Database**: CSV file with customer & real estate data

---

## ✅ Prerequisites

Before starting, ensure you have:
1. **Python 3.8+** installed → [Download](https://www.python.org/downloads/)
2. **Node.js & npm** installed → [Download](https://nodejs.org/)

Verify:
```bash
python --version
node --version
npm --version
```

---

## 🚀 QUICK START (Step-by-Step)

### **STEP 1: Start Backend Server**

**Terminal 1:**
```bash
cd Backend
pip install pandas fastapi uvicorn
python -m uvicorn sort_excel:app --port 5000 --reload
```

✅ Backend running on `http://localhost:5000`

Output should show:
```
Uvicorn running on http://127.0.0.1:5000 (Press CTRL+C to quit)
```

---

### **STEP 2: Start Frontend Server**

**Terminal 2 (NEW TERMINAL):**
```bash
cd TruEstae
npm install
npm run dev
```

✅ Frontend running on `http://localhost:5173`

Output should show:
```
VITE v7.2.5  ready in 150 ms
➜ Local: http://localhost:5173/
```

---

### **STEP 3: Open Application**

Visit in browser:
```
http://localhost:5173/
```

🎉 **Application is now live!**

---

## 🎯 Application Features

### **Search**
- Search by customer name or phone number
- Real-time pagination (20 results per page)

### **Filters** (Apply individually or combined)
- **Region**: Central, North, South, East, West
- **Gender**: Male, Female
- **Age**: 18-25, 26-35, 36-50, 50+
- **Category**: Beauty, Electronic, Clothing
- **Tags**: skincare, beauty, fashion, organic, makeup
- **Payment Method**: Wallet, Cash, Debit Card, Credit Card, UPI, Net Banking
- **Date**: Select specific date
- **Sort By**: Name (A-Z), Name (Z-A), Newest First, Oldest First

### **Pagination**
- Filter pagination: 10 results per page
- Search pagination: 20 results per page
- Next/Previous buttons with auto-disable

---

## 🔧 Backend API Endpoints

### **1. Search Customers by Name/Phone**
```
POST http://localhost:5000/search-name-phone
Content-Type: application/json

{
  "query": "john",
  "page": 1,
  "limit": 20
}

Response:
{
  "total": 45,
  "page": 1,
  "limit": 20,
  "data": [...]
}
```

### **2. Filter & Sort Data**
```
POST http://localhost:5000/get-data
Content-Type: application/json

{
  "region": "North",
  "gender": "Male",
  "age": "26-35",
  "category": "Clothing",
  "tags": "fashion",
  "payment": "Credit Card",
  "date": "2024-01-15",
  "sortBy": "name-asc",
  "page": 1,
  "limit": 10
}

Response: [records...]
```

**Sort Options:**
- `name-asc` - Name A→Z
- `name-desc` - Name Z→A
- `newest` - Latest date first
- `oldest` - Oldest date first

---

## 📁 Project Structure

```
Backend/
├── sort_excel.py                      (FastAPI server)
├── truestate_assignment_dataset.csv   (Data)
└── __pycache__/                       (Cache)

TruEstae/
├── src/
│   ├── App.jsx                    (Main component)
│   ├── main.jsx                   (Entry point)
│   ├── App.css
│   ├── index.css
│   ├── Pages/
│   │   ├── Home.jsx               (Filter/Search UI)
│   │   ├── Table.jsx              (Data display)
│   │   ├── Logo.jsx               (Logo component)
│   │   └── Home.css
│   ├── Components/
│   │   ├── search_bar.jsx         (Search input)
│   │   └── search_bar.css
│   ├── Styles/
│   │   ├── Table.css
│   │   └── Logo.css
│   └── assets/                    (Images, etc.)
├── public/
├── package.json
├── vite.config.js
├── eslint.config.js
└── index.html
```

---

## 📦 Dependencies

### **Backend**
- `fastapi` - Web framework
- `uvicorn` - Server
- `pandas` - Data processing
- `pydantic` - Data validation

### **Frontend**
- `react@19.2.0` - UI library
- `react-dom@19.2.0` - React DOM
- `vite` - Build tool
- `eslint` - Code linter

---

## 🛠️ Available Commands

### **Backend**
```bash
cd Backend

# Development (with auto-reload on port 5000)
python -m uvicorn sort_excel:app --port 5000 --reload

# Production
python -m uvicorn sort_excel:app --port 5000
```

### **Frontend**
```bash
cd TruEstae

# Development server
npm run dev

# Build for production
npm run build

# Preview production build
npm run preview

# Lint code
npm run lint
```

---

##  Troubleshooting

### **Port 5000 in use**
```bash
python -m uvicorn sort_excel:app --port 5001 --reload
# Then update frontend URLs to http://localhost:5001
```

### **Port 5173 in use**
```bash
npm run dev -- --port 5174
```

### **Python packages not found**
```bash
pip install --upgrade pip
pip install pandas fastapi uvicorn
```

### **npm modules error**
```bash
npm cache clean --force
rm -r node_modules
npm install
```

### **CSV file not found (Backend error)**
Ensure `truestate_assignment_dataset.csv` is in the Backend folder

---

## 📊 Data Schema

CSV columns:
- `Customer Name` - Name of customer
- `Phone Number` - Contact number
- `Gender` - Male/Female
- `Age` - Age value
- `Customer Region` - Region location
- `Product Category` - Category
- `Tags` - Product tags
- `Payment Method` - Payment type
- `Date` - Transaction date

---

## ✅ Verification Checklist

- [ ] Python 3.8+ installed
- [ ] Node.js/npm installed
- [ ] Backend dependencies installed: `pip install pandas fastapi uvicorn`
- [ ] Backend running on `http://localhost:5000`
- [ ] Frontend dependencies installed: `npm install`
- [ ] Frontend running on `http://localhost:5173`
- [ ] CSV file exists in Backend folder
- [ ] Can access application at `http://localhost:5173/`
- [ ] Search functionality works
- [ ] Filters work independently
- [ ] Pagination works

---

## 🚀 You're All Set!

Your TruEstae application is ready to use. Both servers must be running simultaneously for the app to work properly.

SandeepYadav18717: give more details of setup instrutio

GitHub Copilot: Read [](file:///c%3A/Users/Administrator/OneDrive/Documents/resumes%20for%20companies/New%20folder/TruEstae/README.md)

Read [](file:///c%3A/Users/Administrator/OneDrive/Documents/resumes%20for%20companies/New%20folder/Backend/sort_excel.py#1-1), lines 1 to 50

Read [](file:///c%3A/Users/Administrator/OneDrive/Documents/resumes%20for%20companies/New%20folder)

# 📋 COMPLETE & DETAILED SETUP INSTRUCTIONS

## 🎯 Project Overview

**TruEstae** is a full-stack real estate management application with:
- **Backend**: Python FastAPI REST API (Port 5000)
- **Frontend**: React 19 + Vite SPA (Port 5173)
- **Data**: CSV-based dataset with customer & property information

---

## ✅ PREREQUISITES - What You Need Installed

### **1. Python Installation**

**For Windows:**
1. Download from: https://www.python.org/downloads/
2. Choose **Python 3.10 or higher** (3.11+ recommended)
3. During installation, **CHECK**: "Add Python to PATH"
4. Click "Install Now"

**Verify installation:**
```bash
python --version
```
Should show: `Python 3.x.x`

### **2. Node.js & npm Installation**

**For Windows:**
1. Download from: https://nodejs.org/
2. Choose **LTS version** (Long Term Support)
3. Run installer and click "Next" through all steps
4. Restart your computer after installation

**Verify installation:**
```bash
node --version
npm --version
```
Should show version numbers like `v18.x.x` and `8.x.x`

### **3. Code Editor (Optional but Recommended)**
- Download VS Code: https://code.visualstudio.com/

---

## 🔧 DETAILED SETUP STEPS

### **PHASE 1: BACKEND SETUP (Python FastAPI)**

#### **Step 1.1: Navigate to Backend Folder**

Open Command Prompt (cmd) or PowerShell and run:
```bash
cd "C:\whole project your parth\Backend" 
# see its ur projectpath whole
```

Or if you're already in the project root:
```bash
cd Backend
```

#### **Step 1.2: Install Python Dependencies**

Run these commands one by one:

```bash
pip install pandas
```
- **What it does**: Installs pandas library for reading/manipulating CSV data

```bash
pip install fastapi
```
- **What it does**: Installs FastAPI framework for creating REST API endpoints

```bash
pip install uvicorn
```
- **What it does**: Installs Uvicorn ASGI server to run FastAPI

**Alternative (install all at once):**
```bash
pip install pandas fastapi uvicorn
```

**Verify installations:**
```bash
pip list
```
Should show: pandas, fastapi, uvicorn in the list

#### **Step 1.3: Start Backend Server**

```bash
python -m uvicorn sort_excel:app --port 5000 --reload
```

**Breakdown:**
- `python -m uvicorn` - Run Uvicorn server
- `sort_excel:app` - Load the app from sort_excel.py file
- `--port 5000` - Run on port 5000
- `--reload` - Auto-restart on code changes

**Expected output:**
```
INFO:     Uvicorn running on http://127.0.0.1:5000 (Press CTRL+C to quit)
INFO:     Application startup complete
```

**Backend is now RUNNING on http://localhost:5000**

**Test the backend:**
Open browser and go to: `http://localhost:5000/docs`
- You should see FastAPI Swagger UI with all API endpoints

---

### **PHASE 2: FRONTEND SETUP (React + Vite)**

#### **Step 2.1: Open NEW Terminal Window**

**IMPORTANT**: Keep backend running! Open a **new Command Prompt/PowerShell window**

In the new terminal:
```bash
cd "C:\Users\Administrator\OneDrive\Documents\resumes for companies\New folder\TruEstae"
```

Or:
```bash
cd TruEstae
```

#### **Step 2.2: Install Frontend Dependencies**

```bash
npm install
```

**What it does:**
- Reads package.json
- Downloads and installs all packages:
  - `react@19.2.0`
  - `react-dom@19.2.0`
  - `vite`
  - `eslint`
  - All other dev dependencies

**This will take 1-2 minutes** (creates `node_modules` folder ~500MB)

**Verify:**
```bash
npm --version
```

#### **Step 2.3: Start Frontend Development Server**

```bash
npm run dev
```

**What it does:**
- Starts Vite development server
- Enables hot module reloading (HMR)
- Watches for file changes

**Expected output:**
```
VITE v7.2.5  ready in 200 ms

➜ Local: http://localhost:5173/
➜ press h to show help
```

✅ **Frontend is now RUNNING on http://localhost:5173**

---

##  ACCESSING THE APPLICATION

### **Open in Browser:**
```
http://localhost:5173/
```

You should see:
- TruEstae logo
- Search bar
- Filter dropdowns (Region, Gender, Age, Category, etc.)
- Data table with results
- Pagination controls

---

## 📊 WHAT EACH COMPONENT DOES

### **Backend Files**

| File | Purpose |
|------|---------|
| sort_excel.py | Main FastAPI server with 2 endpoints |
| `truestate_assignment_dataset.csv` | Database (customer data) |

**Endpoints in sort_excel.py:**

1. **POST `/search-name-phone`**
   - Search customers by name or phone number
   - Takes query string as input
   - Returns paginated results (20 per page)

2. **POST `/get-data`**
   - Filter and sort data
   - Accepts multiple filters (region, gender, age, etc.)
   - Returns paginated results (10 per page)
   - Uses Merge Sort algorithm for sorting

### **Frontend Files**

| File | Purpose |
|------|---------|
| App.jsx | Root component, imports Home |
| `src/main.jsx` | Entry point, renders App |
| Home.jsx | Main page with filters, search, table |
| `src/Pages/Table.jsx` | Displays data in table format |
| `src/Pages/Logo.jsx` | Logo component |
| `src/Components/search_bar.jsx` | Search input component |
| `src/Styles/` | CSS styling files |

---

##  FILTER OPTIONS

When using the application, you can filter by:

| Filter | Options |
|--------|---------|
| **Region** | Central, North, South, East, West |
| **Gender** | Male, Female |
| **Age** | 18-25, 26-35, 36-50, 50+ |
| **Category** | Beauty, Electronic, Clothing |
| **Tags** | skincare, beauty, fashion, organic, makeup |
| **Payment** | Wallet, Cash, Debit Card, Credit Card, UPI, Net Banking |
| **Date** | Select any date (date picker) |
| **Sort** | Name A-Z, Name Z-A, Newest, Oldest |

---

## 🔌 API ENDPOINTS DETAILS

### **Endpoint 1: Search by Name/Phone**

```
URL: POST http://localhost:5000/search-name-phone
```

**Request Body:**
```json
{
  "query": "john",
  "page": 1,
  "limit": 20
}
```

**Parameters:**
- `query` (required) - Search term (name or phone)
- `page` (optional, default=1) - Page number
- `limit` (optional, default=20) - Results per page

**Response:**
```json
{
  "total": 45,
  "page": 1,
  "limit": 20,
  "data": [
    {
      "Customer Name": "John Doe",
      "Phone Number": "9876543210",
      "Gender": "Male",
      "Age": 28,
      ...
    }
  ]
}
```

---

### **Endpoint 2: Filter & Sort Data**

```
URL: POST http://localhost:5000/get-data
```

**Request Body:**
```json
{
  "region": "North",
  "gender": "Male",
  "age": "26-35",
  "category": "Clothing",
  "tags": "fashion",
  "payment": "Credit Card",
  "date": "2024-01-15",
  "sortBy": "name-asc",
  "page": 1,
  "limit": 10
}
```

**Parameters:**
- `region` - Filter by region
- `gender` - Filter by gender
- `age` - Filter by age range (e.g., "18-25")
- `category` - Filter by product category
- `tags` - Filter by tags
- `payment` - Filter by payment method
- `date` - Filter by specific date (format: YYYY-MM-DD)
- `sortBy` - Sort option (see below)
- `page` - Page number (default=1)
- `limit` - Results per page (default=10)

**Sort Options:**
- `name-asc` - Sort by customer name A to Z
- `name-desc` - Sort by customer name Z to A
- `newest` - Sort by date (newest first)
- `oldest` - Sort by date (oldest first)

**Response:**
```json
[
  {
    "Customer Name": "Alice Smith",
    "Phone Number": "9123456789",
    "Gender": "Female",
    "Age": 32,
    "Customer Region": "North",
    "Product Category": "Clothing",
    "Tags": "fashion",
    "Payment Method": "Credit Card",
    "Date": "2024-01-15"
  }
]
```

---

## 🚀 QUICK REFERENCE COMMANDS

### **Backend (Terminal 1)**
```bash
# Navigate to backend
cd Backend

# Install dependencies
pip install pandas fastapi uvicorn

# Start server (Port 5000)
python -m uvicorn sort_excel:app --port 5000 --reload

# Stop: Press Ctrl+C
```

### **Frontend (Terminal 2)**
```bash
# Navigate to frontend
cd TruEstae

# Install dependencies (first time only)
npm install

# Start development server (Port 5173)
npm run dev

# Stop: Press Ctrl+C
```

---

## 📁 FILE STRUCTURE EXPLAINED

```
Project Root/
│
├── Backend/
│   ├── sort_excel.py              ← FastAPI server (READ THIS FIRST)
│   ├── truestate_assignment_dataset.csv  ← Data file (must exist)
│   └── __pycache__/               ← Python cache (auto-generated)
│
└── TruEstae/                       ← React app folder
    ├── src/
    │   ├── main.jsx               ← Renders React app
    │   ├── App.jsx                ← Root component
    │   ├── App.css                ← Global styles
    │   ├── index.css              ← Base CSS
    │   ├── Pages/
    │   │   ├── Home.jsx           ← Main page (filters + search)
    │   │   ├── Table.jsx          ← Data table component
    │   │   ├── Logo.jsx           ← Logo display
    │   │   └── *.css              ← Page styles
    │   ├── Components/
    │   │   ├── search_bar.jsx     ← Search input
    │   │   └── search_bar.css
    │   ├── Styles/                ← CSS files
    │   │   ├── Home.css
    │   │   ├── Table.css
    │   │   └── Logo.css
    │   └── assets/                ← Images, fonts
    ├── public/                    ← Static files
    ├── package.json               ← Dependencies list
    ├── vite.config.js             ← Vite configuration
    ├── eslint.config.js           ← Linting rules
    └── index.html                 ← HTML template
```

---

##  COMMON ISSUES & SOLUTIONS

### **Issue 1: "Python not found"**
```
Error: 'python' is not recognized
```
**Solution:**
- Python not in PATH
- Reinstall Python and check "Add Python to PATH"
- Use `python3` instead of `python`

### **Issue 2: "Module not found: pandas/fastapi"**
```
Error: No module named 'pandas'
```
**Solution:**
```bash
pip install --upgrade pip
pip install pandas fastapi uvicorn
```

### **Issue 3: Port 5000 already in use**
```
Error: Address already in use
```
**Solution:**
```bash
# Use different port
python -m uvicorn sort_excel:app --port 5001 --reload
# Then update frontend to use 5001
```

### **Issue 4: Port 5173 already in use**
```bash
npm run dev -- --port 5174
```

### **Issue 5: CSV file not found**
```
Error: FileNotFoundError: 'truestate_assignment_dataset.csv'
```
**Solution:**
- Ensure CSV file is in Backend folder
- Check file name is spelled correctly
- File should be in same folder as sort_excel.py

### **Issue 6: npm modules error**
```bash
# Clear cache
npm cache clean --force

# Delete node_modules
rmdir /s /q node_modules

# Reinstall
npm install
```

### **Issue 7: Cannot connect to backend from frontend**
- Ensure backend is running on port 5000
- Check Home.jsx has correct URL: `http://localhost:5000`
- Check CORS is enabled in sort_excel.py  (it is)

---

## ✅ VERIFICATION CHECKLIST

Before considering setup complete:

- [ ] Python 3.8+ installed and in PATH
- [ ] Node.js v14+ and npm installed
- [ ] Backend folder has sort_excel.py and CSV file
- [ ] Ran `pip install pandas fastapi uvicorn`
- [ ] Backend running on `http://localhost:5000`
- [ ] Can access `http://localhost:5000/docs` (Swagger UI)
- [ ] Ran `npm install` in TruEstae folder
- [ ] Frontend running on `http://localhost:5173`
- [ ] Can see TruEstae app in browser at `http://localhost:5173`
- [ ] Search functionality works
- [ ] Filters work and update table
- [ ] Pagination works
- [ ] No browser console errors

---

## 🎯 WORKFLOW SUMMARY

```
1. Open Terminal 1 → cd Backend
   ↓
2. pip install pandas fastapi uvicorn
   ↓
3. python -m uvicorn sort_excel:app --port 5000 --reload
   ↓ Backend running ✓
   
4. Open Terminal 2 → cd TruEstae
   ↓
5. npm install
   ↓
6. npm run dev
   ↓ Frontend running ✓
   
7. Open Browser → http://localhost:5173/
   ↓
8. Use the application!
```

---


---

