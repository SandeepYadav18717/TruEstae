Receiving Filter Input
The frontend sends filter selections in a JSON format.

Converting Data to List Format
The CSV file is converted from a pandas DataFrame to a list of dictionaries

Filter Functions (Condition Checkers)
Each filter condition has a dedicated Python function that checks whether a particular row matches the filter.

Applying Filters Using Normal Python Loops
The backend checks which filters the user has provided, and applies only those conditions.